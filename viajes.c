
#include <stdio.h>
#include <stdlib.h>
#include "viajes.h"






int main()
{


    if((*m_viajes = (m_viajes*)malloc(l_Viajes*sizeof(viajes)))==NULL) puts("no hay espacio suficiente");
    return 0;
}
